% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{predict_two_stage}
\alias{predict_two_stage}
\title{Predict outcomes using two-stage network regression}
\usage{
predict_two_stage(A, X_test, ret_two_stage, scaled = 1)
}
\arguments{
\item{A}{Full adjacency matrix containing both training and test nodes}

\item{X_test}{Covariate matrix for the test set}

\item{ret_two_stage}{A fitted object from \code{two_stage()}}

\item{scaled}{Logical; whether to scale test singular vectors to match training norm (default \code{TRUE})}
}
\value{
A list with predicted outcomes \code{y}, test centralities \code{u} and \code{v}
}
\description{
Applies the estimated coefficients from a two-stage network regression model to test data.
}
\examples{
# Assume last 20 rows/cols of A correspond to test nodes
pred <- predict_two_stage(A, X_test, ret)

}
\seealso{
\code{\link{two_stage}}, \code{\link{predict_supervised}}
}
